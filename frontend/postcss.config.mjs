// Kept local to this package. Vite uses an inline empty plugin list so a
// machine-global PostCSS file (for example C:\postcss.config.mjs) cannot
// change the production build.
export default {
  plugins: {},
}
